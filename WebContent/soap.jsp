<%@include file="header.jsp"%>
<article>
	<div>
	<!--  --><a href="restful.jsp" class="previous">&laquo; Previous</a>
<a href="#" class="next">Next &raquo;</a></div>
		<h1 class="entry-title">SOAP Web Service</h1>
		<p><b>Web-Srevices:</b>This provides services with web. That means It provides services to create communication between different applications over the network.This is mainly Used for InterOperability.
		One Application can use the methods and functions of another application. You can written these two applications in two different technologies. Then WebService help us to sare data from one to another.</p>
		<h3>Main Things</h3>
		<p>UDDI(Universal Description discovery Interface)
		<p>WSDL(WebService Description Language)
		<p>SOAP(Simple Object Access Protocol)
		<p>SEI(Service End point Interface)</p>
		
		<h3>UDDI:</h3><p> UDDI is Directory.Publisher can publish Web Services into it. And Consumer can create Query to this directory to access WebSevices.</p> 
		<h3>WSDL</h3><p>This is an XML based language which describes WebServices.</p>
		<h3>SEI</h3><p>Service EndPoint interface is used to provides way to client application irrespective of technology.
	
		<p><b>SOAP:</b> SOAP is a Simple Object Access Protocol.This protocol is used to access the Web Services.</p>
		<p>SOAP is a application communication protocol.</p>
		<p>SOAP is Platform Independent</p>
		<p> It is one of the webService used to provide interOperability.</p>
		<p>SOAP is basically a protocol, Language, XML format.</p>
		<p>SOAP uses XML language for exchanging messages.Exchanging means encoding and decoding messages.</p>
		<p>SOAP uses different Transport Protocols such as HTTP(Hyper Text Transfer Protocol) and SMTP(Simple Mail Transfer Protocol)
		<p>HTTP Protocol is a Stateless Protocol.</p>
		
		
		
		
		
		
		
		</article>
	
<%@include file="footer.jsp"%>